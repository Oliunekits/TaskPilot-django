services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      DJANGO_DEBUG: "0"
      DJANGO_SECRET_KEY: "change-me-in-prod"
    volumes:
      - sqlite_data:/app/data

volumes:
  sqlite_data:
